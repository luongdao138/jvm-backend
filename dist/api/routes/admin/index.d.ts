import { Router } from 'express';
import { ConfigModule } from '../../../types';
import { AppContainer } from '../../../utils';
export default function (masterRoute: Router, container: AppContainer, config: ConfigModule): Router;
